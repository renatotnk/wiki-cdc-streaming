# Progress

**Current phase:** Phase 3 — Silver (`docs/SPEC-phase3-silver-cdf.md`), on branch `phase3-silver`.

**Implemented:** `silver_recentchange`/`silver_recentchange_rejected` (Python + SQL, `_py`/`_sql` suffixed) via a shared internal fan-out table `silver_recentchange_staging` (bronze's CDF read/flattened/deduped/tagged once, both final tables filter its `_dq_failure_reasons` column). `pipelines/silver/dq_rules.py` implements the 6 DQ dimensions as plain PySpark/SQL rule logic, not SDP expectations (re-confirmed they don't exist in OSS Spark — Phase 2's own finding). `src/shared/text_normalization.py` (`normalize_nfc`, Python variant only) and `src/shared/contract_loader.py` (module-level P8 fail-fast check). Several new discovered constraints beyond Phase 2's, all documented in `docs/SPEC-phase3-silver-cdf.md` Section 4.1–4.4 and `docs/trade-offs.md`: `ROW_NUMBER()` window functions are disallowed on streaming Datasets (`dropDuplicatesWithinWatermark` instead, with its own disclosed uniqueness-traceability gap); a same-run cross-table reference only resolves if the referenced table was materialized by an earlier, separate `spark-pipelines run` (`pipelines/spark-pipeline.bronze-only.yml`, generated alongside the main spec, exists for this one-time bootstrap); `CREATE STREAMING TABLE` not `CREATE OR REFRESH STREAMING TABLE`; UDF registration is blocked from inside any pipeline file (the SQL variant doesn't NFC-normalize); `spark.sql.session.timeZone` must be pinned to UTC pipeline-wide. Tests: `tests/test_text_normalization.py`, `tests/test_dq_expectations.py`, `tests/test_silver_pipeline.py` (bronze-only bootstrap + combined run, P4 reconciliation, rejection traceability, multilingual survival, late-arrival flagging, Python/SQL equivalence). `docs/RUNBOOK.md`, `docs/CLAUDE.md`, `README.md`, `docs/trade-offs.md`, `requirements.txt` updated accordingly.

**Left:** User is reviewing before anything is committed. Once approved: commit, push `phase3-silver`, open a PR, merge to `main` — then start Phase 4 (`docs/SPEC-phase4-gold-consumption.md`).
