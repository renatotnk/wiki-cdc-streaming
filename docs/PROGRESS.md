# Progress

**Current phase:** Phase 2.5 — CI/CD (`docs/SPEC-phase2__5-cicd.md`), on branch `phase2-5-cicd`.

**Implemented:** `.github/workflows/ci.yml` -- `test` job (Python 3.12 + Java 17 setup, local stack via `docker-compose`, `spark-pipelines dry-run` before a real run, `pytest tests/`, teardown via `if: always()`) declares zero cloud secrets; `deploy` job runs only via manual `workflow_dispatch` with `confirm_deploy=true`, gated behind `DATABRICKS_HOST`/`DATABRICKS_TOKEN` secrets the `test` job never references. `src/handlers/deploy/databricks_jobs_handler.py` (`DatabricksJobsHandler`) + `scripts/deploy_databricks_job.py` (the `--confirm`-gated entry point). `tests/fixtures/sample_recentchange_events.ndjson` (50 events: a bot event, a `log`-type event, a deliberate duplicate `_event_id`) + `tests/doubles/fake_wiki_events_handler.py` (`FakeWikiEventsHandler`, mirrors the real handler's `events()` contract) + `tests/test_fake_wiki_events_handler.py` (fixture content + dedup integration, no docker/network needed). Verified against a genuinely fresh local stack end-to-end: all 28 tests pass together (`docker compose up` → render config → seed `raw/`+`dim_wiki_reference/` → dry-run → pytest → `docker compose down -v`). Two real constraints discovered doing that (documented in `docs/RUNBOOK.md` Phase 2.5 and `docs/CLAUDE.md`): a genuinely empty bucket needs a seeded file under both `raw/` and `dim_wiki_reference/` before the first dry-run/run (not just the latter, as Phase 2's SPEC recorded), and `tests/test_ingestion_smoke.py` had to stop concatenating full raw-file schemas across test modules that don't share a fixture shape. `docs/RUNBOOK.md`, `docs/CLAUDE.md`, `README.md`, `.env.example` updated accordingly.

**Left:** User is reviewing before anything is committed. Once approved: commit, push `phase2-5-cicd`, open a PR (first PR to actually exercise the new CI workflow), merge to `main` — then start Phase 3 (`docs/SPEC-phase3-silver-cdf.md`).
